const loginHandler = (request, h) => {
  const {
    username,
    passwd,
  } = request.payload;



};

const registrationHandler = (request, h) => {
  const {
    username,
    passwd,
  } = request.payload

  const id = crypto.randomUUID();
};

const aiHandler = () => {};

const suggestionHandler = () => {};